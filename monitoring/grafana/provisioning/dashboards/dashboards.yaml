apiVersion: 1

providers:
  - name: FraudLens
    orgId: 1
    folder: FraudLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
